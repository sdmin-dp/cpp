#include<bits/stdc++.h>
#define ll long long
using namespace std;
#define el '\n'
const ll N=1e5+5;
ll n1,n2,m;
vector<ll> g[N];
ll match[N];
ll vis[N];
bool dfs(ll x){
    
}
void solve(){
    cin>>n1>>n2>>m;
    for(int i=1;i<=m;i++){
        ll x,y;
        g[x].push_back(y);
    }
}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    //freopen("xxx.in","r",stdin);
    //freopen("xxx.out","w",stdout);
    ll T=1;
    //cin>>T;
    while(T--){
        solve();
    }
    return 0;
}