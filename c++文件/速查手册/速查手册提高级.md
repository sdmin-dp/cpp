# C++ 提高级速查手册：进阶「武功秘籍」 🌟✨

欢迎来到 C++ 提高级速查手册！这里是你的「进阶武功秘籍」，包含了 CSP-S 和 NOIP 的核心算法、数学工具和程序模板。无论你是「小试牛刀」还是「冲击金牌」，这里都能帮你快速找到需要的「武功秘籍」！

---

## 使用说明：如何成为「武林高手」

1. **目录**：左边的目录可以快速跳转到你需要的章节。
2. **搜索**：右上角的搜索框可以帮你快速找到关键字。
3. **收藏**：把常用的模板收藏起来，随时复习。

**互动练习**：
- 打开目录，找到「第二章 分治、扫描线与离线思想」，看看你能不能快速找到答案。

---

## 第一章 类、结构体与算法封装：编程的「组合拳」 🥋

类和结构体就是编程的「组合拳」，可以把数据和操作「打包」在一起。

### 类和结构体：数据的「百宝箱」

**例子**：
```cpp
struct Point {
    int x, y;
    Point(int x = 0, int y = 0) : x(x), y(y) {}
    void print() {
        cout << "(" << x << ", " << y << ")";
    }
};
```

**互动练习**：
- 定义一个 `Student` 结构体，包含姓名、年龄和成绩，并实现一个打印信息的方法。

---

### 算法封装：功能的「组合拳」

**例子**：
```cpp
int binarySearch(vector<int>& nums, int target) {
    int left = 0, right = nums.size() - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) return mid;
        else if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

**互动练习**：
- 实现一个函数，用二分查找在有序数组中查找目标值。

---

## 第二章 分治、扫描线与离线思想：编程的「分身术」 🎭

分治、扫描线和离线思想就是编程的「分身术」，可以把复杂的问题「分而治之」。

### 分治：问题的「分身」

**例子**：
```cpp
void mergeSort(vector<int>& nums, int left, int right) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;
    mergeSort(nums, left, mid);
    mergeSort(nums, mid + 1, right);
    merge(nums, left, mid, right);
}
```

**互动练习**：
- 用归并排序对 `{5, 3, 8, 4, 2}` 进行排序。

---

### 扫描线：问题的「扫描」

**例子**：
```cpp
struct Event {
    int x, type, y1, y2;
    Event(int x, int type, int y1, int y2) : x(x), type(type), y1(y1), y2(y2) {}
    bool operator<(const Event& other) const {
        return x < other.x;
    }
};
```

**互动练习**：
- 用扫描线算法计算矩形面积并。

---

### 离线思想：问题的「预处理」

**例子**：
```cpp
vector<int> ans(q);
sort(queries.begin(), queries.end(), [](const Query& a, const Query& b) {
    return a.r < b.r;
});
```

**互动练习**：
- 用离线思想处理区间查询问题。

---

## 第三章 单调结构与哈希表：编程的「快速通道」 🚀

单调结构和哈希表就是编程的「快速通道」，可以快速查找和处理数据。

### 单调栈：数据的「单调性」

**例子**：
```cpp
vector<int> nextGreaterElement(vector<int>& nums) {
    vector<int> ans(nums.size(), -1);
    stack<int> s;
    for (int i = 0; i < nums.size(); i++) {
        while (!s.empty() && nums[s.top()] < nums[i]) {
            ans[s.top()] = nums[i];
            s.pop();
        }
        s.push(i);
    }
    return ans;
}
```

**互动练习**：
- 用单调栈找出数组中每个元素的下一个更大元素。

---

### 哈希表：数据的「快速查找」

**例子**：
```cpp
unordered_map<string, int> freq;
for (string& word : words) {
    freq[word]++;
}
```

**互动练习**：
- 用哈希表统计单词出现的频率。

---

## 第四章 搜索进阶：编程的「探险之旅」 🗺️

搜索进阶就是编程的「探险之旅」，可以探索未知的「领域」。

### DFS 和 BFS：探索的「路线」

**例子**：
```cpp
void dfs(int u) {
    visited[u] = true;
    for (int v : adj[u]) {
        if (!visited[v]) {
            dfs(v);
        }
    }
}
```

**互动练习**：
- 用 DFS 和 BFS 遍历一个图。

---

### 启发式搜索：探索的「智慧」

**例子**：
```cpp
priority_queue<Node, vector<Node>, greater<Node>> pq;
pq.push({0, start});
while (!pq.empty()) {
    Node node = pq.top();
    pq.pop();
    if (node.pos == target) return node.cost;
    for (auto& neighbor : adj[node.pos]) {
        pq.push({node.cost + neighbor.cost, neighbor.pos});
    }
}
```

**互动练习**：
- 用 A* 算法寻找最短路径。

---

## 第五章 动态规划进阶：编程的「智慧之光」 💡

动态规划进阶就是编程的「智慧之光」，可以解决复杂的「优化问题」。

### 背包问题：选择的「艺术」

**例子**：
```cpp
for (int i = 1; i <= n; i++) {
    for (int j = W; j >= w[i]; j--) {
        dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
    }
}
```

**互动练习**：
- 用背包问题解决「0-1 背包」问题。

---

### 区间 DP：区间的「智慧」

**例子**：
```cpp
for (int len = 2; len <= n; len++) {
    for (int i = 1; i <= n - len + 1; i++) {
        int j = i + len - 1;
        for (int k = i; k < j; k++) {
            dp[i][j] = max(dp[i][j], dp[i][k] + dp[k + 1][j] + cost[i][j]);
        }
    }
}
```

**互动练习**：
- 用区间 DP 解决「石子合并」问题。

---

## 第六章 最短路与图上路径进阶：编程的「导航系统」 🧭

最短路和图上路径进阶就是编程的「导航系统」，可以找到「最优路径」。

### Dijkstra 算法：最短路的「导航」

**例子**：
```cpp
priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
pq.push({0, start});
dist[start] = 0;
while (!pq.empty()) {
    auto [d, u] = pq.top();
    pq.pop();
    if (d > dist[u]) continue;
    for (auto& [v, w] : adj[u]) {
        if (dist[v] > dist[u] + w) {
            dist[v] = dist[u] + w;
            pq.push({dist[v], v});
        }
    }
}
```

**互动练习**：
- 用 Dijkstra 算法求单源最短路。

---

### Floyd 算法：全源最短路的「导航」

**例子**：
```cpp
for (int k = 1; k <= n; k++) {
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
        }
    }
}
```

**互动练习**：
- 用 Floyd 算法求全源最短路。

---

## 总结

C++ 提高级速查手册就是你的「进阶武功秘籍」，里面有类、结构体、分治、扫描线、单调结构、哈希表、搜索进阶、动态规划进阶、最短路等「武功秘籍」。学会了它们，你就能成为编程的「武林高手」！

快去试试这些互动练习吧，看看你能不能成为「编程大师」！ 🧙‍♂️💻