#include<bits/stdc++.h>
#define ll long long
using namespace std;
#define el '\n'
const ll N=1e5+5;

void solve(){
    
}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    //freopen("xxx.in","r",stdin);
    //freopen("xxx.out","w",stdout);
    ll T=1;
    //cin>>T;
    while(T--){
        solve();
    }
    return 0;
}
/*
{
    // ... 其他配置不动 ...
    
    // 1. 关闭保存时自动格式化（这是最关键的）
    "editor.formatOnSave": false, 

    // 2. 如果你还想彻底点，把这些也关了（可选）
    "editor.formatOnPaste": false,      // 粘贴时不自动格式化
    "editor.formatOnType": false,       // 打字时不自动格式化

    // 你配置里原本有的这两行，也可以直接删掉或者留着（只要上面关了就不生效）
    "editor.defaultFormatter": null,
    "notebook.defaultFormatter": null
}
*/